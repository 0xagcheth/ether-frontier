# Ether Frontier — Symbol Candidate Review v3

Status: ImageGen 2 candidate review, not canon assets  
Updated: 2026-07-23

## Fatta v3

File: `assets/staging/candidates/symbols/fatta_symbol_v3_imagegen2.png`

The candidate successfully removes concentric rings and explicit rotational
symmetry. Two offset arcs and detached calibration block are readable.

Remaining issue: the composition resembles two linked chain loops or a stylized
question-mark pair more than an orbital regulator.

Decision: **ITERATE**, not rejected for safety but not identity-ready.

Next prompt must shorten both arcs further and introduce three unmistakable
static bridge blocks between different rails rather than tangential contacts.

## Kitezh-17 v3

File: `assets/staging/candidates/symbols/kitezh17_symbol_v3_imagegen2.png`

The candidate removes the crosshair and complete file outline.

Remaining issue: parallel inner lines and the large circle read as an information
document/list icon. This is still generic interface language.

Decision: **REJECT v3**.

Next direction must abandon archive-document geometry entirely. Build the sign
from a buried-site section: one offset site ring cut by three horizontal
stratigraphic plates and one external inventory tab, with no enclosing bracket.

## Model compliance

Both images were generated with built-in ImageGen 2 and copied into the project.
No manual raster or SVG substitution was used for these artistic candidates.
